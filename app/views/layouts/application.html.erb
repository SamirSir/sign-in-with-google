<!DOCTYPE html>
<html>
  <head>
    <title>SignWithGoogle</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <style>
        .flash__alert   { color: red; }
        .flash__notice  { color: blue; }
        .flash__success { color: green; }
        .flash_xyz  {color: purple;}
    </style>
  </head>

  <body>

    <% if flash.any? %>
      <% flash.each do |key, value| -%>
        <section class = "flash_<%= key %>"><%= value.html_safe %></section>
      <% end %>
    <% end %>

    <h1>You are Welcome Everywhere!</h1>
    <hr>
    <%= yield %>
  </body>
</html>
